DATABASE_URL="postgresql://postgres:postgres@localhost:5432/uptime?schema=public"

# Must match a Region.name row in the database (the seed script creates "asia").
REGION_ID="asia"

# Optional - if unset, a unique id is derived from hostname+pid.
# WORKER_ID="worker-1"
